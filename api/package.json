{
  "name": "cubeforge-api",
  "version": "1.0.0",
  "description": "CubeForge Central API - Server discovery and session management",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260827.1",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.0"
  }
}
